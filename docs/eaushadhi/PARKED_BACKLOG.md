# E-AUSHADHI AUTOMATION — PARKED BACKLOG

This file is the scope sink for useful but non-blocking discoveries. Items stay parked unless formally promoted into an active work pack because they become acceptance blockers.

## Current parked items
- Rebase→Resume hydration UX improvements.
- True pageno>0 pagination if catalogue exceeds current practical bounds.
- Broader renderer candidateId/runId/requestedId/loadedHiddenId cleanup in unrelated historical failure paths.
- Attachment reread-unavailability semantic refinement.
- Capture-engine refinement for page-specific static-JS priority/cap.
- Isolated old feature/hotfix worktree cleanup where untracked smoke userdata prevents safe normal removal; no force clean.
- Potential reduction of repeated shared Reference presentation where audit clarity is already sufficient.
- Additional dashboard/analytics polish not required for pipeline operation.
- Future Therapeutic Guide functionality and richer canonical-data reuse.
- Non-blocking keyboard/productivity refinements after the core operator workflows are usable.

## Promotion rule
An item may leave this backlog only when ChatGPT documents:
1. the work pack it belongs to;
2. why it has become required for that work-pack acceptance;
3. dependency/contract impact;
4. updated MASTER_PROGRAMME gate if material.
